node_modules/
*.log
.DS_Store
state/
*.tmp
__pycache__/
*.pyc

# secrets / env — never commit
.env
.env.*
*.token
token.txt

# agent / workflow scratch — never commit or ship
.superpowers/

# build artifacts — publish from CI/local, don't track
*.vsix

# hardware docs / STEP model / datasheets — kept local only, never committed or shipped
hardware/

# personal scratch files, never ship or commit
*shopping*
Overlord - *.txt

# vendored external reference code for local diffing only - NEVER commit
fork-ref/
.claude/
